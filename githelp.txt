
git diff # показать разницу (проиндексированные)
	# -cached # разница (для коммитов)

git rm  # удаление
	# --cached readme.txt #  удаление только из индекса

git mv file_from file_to # переименование, перемещение

git log # просмотр истории коммитов
	# -p # показывает разницу
	# --word-diff # разница по словам
	# -U1 --word-diff #  то же самое
	# -2 # ограничить число записей до 2-х
	# --stat # статистика
	# --shortstat #
	# --name-only # показывать список измененных файлов
	# --name-status # список измененных файлов с информацие о добавлении изменении удалении
	# --abbrev-commit # первые несколько символов контрольной суммы
	# --relative-date # дата в относительном формате
	# --pretty # - формат вывода лога и его параметры:
		#=oneline #
		#=short   #
		#=full    #
		#=fuller  #
		#=format:"%h - %an, %ar : %s" #
	# --graph # используется с "--pretty=oneline" рисует граф
	ОГРАНИЧИТЕЛИ:
	# --since=2.weeks, --after # коммиты после указанной даты
	# --until="2008-10-01", --before #
	# --author, --commiter # Сортировка по автору
	# --grep # поиск по ключевым словам
	# --all-match # если используется grep и author (Должны выполняться оба критерия)
	# -- <path> # можно укать путь



ПСЕВДОНИМЫ

git config --global alias.co checkout
