список веток:
git branch

последний коммит на каждой из веток
git branch -v

ветки, которые слили с текущей
git branch --merged
не слили
git branch --no-merged

Удалить ветку и потерять наработки
git branch -D testing
